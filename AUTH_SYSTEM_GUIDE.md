# 🔐 دليل نظام المصادقة والصلاحيات

## 🎯 نظرة عامة

تم إنشاء نظام مصادقة شامل مع صلاحيات المدير والمستخدم، يتضمن:
- تسجيل الدخول والخروج
- إنشاء حسابات جديدة
- إدارة الجلسات
- صلاحيات متدرجة (Admin/User)
- لوحة تحكم المدير

## 🏗️ هيكل النظام

### 📁 **الملفات الجديدة:**
```
src/
├── utils/auth.js              # وظائف المصادقة الأساسية
├── store/useAuthStore.js      # إدارة حالة المصادقة
├── pages/Login.jsx            # صفحة تسجيل الدخول
├── pages/Register.jsx         # صفحة إنشاء الحساب
├── pages/Admin/
│   └── AdminDashboard.jsx     # لوحة تحكم المدير
├── components/Auth/
│   └── ProtectedRoute.jsx     # حماية المسارات
└── ...

users.json                     # ملف المستخدمين
sessions.json                  # ملف الجلسات (ينشأ تلقائياً)
```

## 👥 أنواع المستخدمين

### 🔴 **المدير (Admin):**
- **الصلاحيات**: جميع الصلاحيات
- **الوصول**: جميع الصفحات + لوحة تحكم المدير
- **المهام**: إدارة المستخدمين، إنشاء التراخيص، إعدادات النظام

### 🔵 **المستخدم العادي (User):**
- **الصلاحيات**: محدودة
- **الوصول**: الصفحات الأساسية فقط
- **المهام**: إدارة المنتجات، المبيعات، التقارير

## 🔑 بيانات الدخول الافتراضية

### **المدير:**
```
اسم المستخدم: admin
كلمة المرور: admin123
البريد الإلكتروني: admin@marketmanager.com
```

### **المستخدم:**
```
اسم المستخدم: user
كلمة المرور: user123
البريد الإلكتروني: user@marketmanager.com
```

## 🚀 كيفية الاستخدام

### 1. **تشغيل التطبيق:**
```bash
npx electron public/electron.js
```

### 2. **تسجيل الدخول:**
- ستظهر صفحة تسجيل الدخول تلقائياً
- أدخل بيانات المدير أو المستخدم
- اضغط "تسجيل الدخول"

### 3. **إنشاء حساب جديد:**
- اضغط "إنشاء حساب جديد" في صفحة تسجيل الدخول
- املأ البيانات المطلوبة
- الحسابات الجديدة تحصل على دور "مستخدم" افتراضياً

### 4. **الوصول لصفحات المدير:**
- سجل دخول كمدير
- اذهب إلى `/admin` أو `/admin/dashboard`

## 🛡️ نظام الحماية

### **ProtectedRoute Component:**
```jsx
// حماية صفحة تتطلب تسجيل دخول
<ProtectedRoute requireAuth={true}>
  <Dashboard />
</ProtectedRoute>

// حماية صفحة تتطلب صلاحيات مدير
<ProtectedRoute requireAuth={true} requireAdmin={true}>
  <AdminDashboard />
</ProtectedRoute>

// صفحة عامة (تسجيل دخول/تسجيل)
<ProtectedRoute requireAuth={false}>
  <Login />
</ProtectedRoute>
```

### **Permission Guards:**
```jsx
// التحقق من الصلاحيات داخل المكونات
<PermissionGuard permission="manage_users">
  <UserManagementButton />
</PermissionGuard>

// عرض محتوى مختلف حسب الدور
<RoleBasedComponent
  adminComponent={<AdminPanel />}
  userComponent={<UserPanel />}
  guestComponent={<LoginPrompt />}
/>
```

## 🔧 الوظائف المتاحة

### **للمدير:**
- ✅ **إدارة المستخدمين**: عرض، تعديل، حذف، تفعيل/إلغاء تفعيل
- ✅ **إدارة التراخيص**: إنشاء، تعديل، حذف، تمديد
- ✅ **إحصائيات النظام**: عدد المستخدمين، التراخيص، النشاطات
- ✅ **تقارير متقدمة**: تحليلات شاملة للنظام
- ✅ **إعدادات النظام**: تكوين النظام العام

### **للمستخدم العادي:**
- ✅ **إدارة المنتجات**: إضافة، تعديل، حذف المنتجات
- ✅ **إدارة المبيعات**: تسجيل المبيعات والفواتير
- ✅ **التقارير الأساسية**: تقارير المبيعات والأرباح
- ✅ **الإعدادات الشخصية**: إعدادات المتجر والحساب

## 📊 لوحة تحكم المدير

### **الإحصائيات:**
- إجمالي المستخدمين
- المستخدمين النشطين
- إجمالي التراخيص
- التراخيص المنتهية قريباً

### **الإجراءات السريعة:**
- إدارة المستخدمين
- إدارة التراخيص
- إنشاء ترخيص جديد
- تقارير النظام
- إعدادات النظام
- صحة النظام

### **النشاطات الأخيرة:**
- تسجيلات الدخول
- إنشاء حسابات جديدة
- تفعيل/إلغاء تفعيل المستخدمين
- إنشاء/تعديل التراخيص

## 🔐 الأمان

### **تشفير كلمات المرور:**
```javascript
// استخدام PBKDF2 مع Salt
const { hash, salt } = hashPassword(password);
// Hash: 64 byte hex string
// Salt: 16 byte random hex string
```

### **إدارة الجلسات:**
```javascript
// رمز جلسة عشوائي 32 byte
const sessionToken = crypto.randomBytes(32).toString('hex');

// انتهاء الجلسة بعد 24 ساعة
expiresAt: new Date(Date.now() + 24 * 60 * 60 * 1000)
```

### **التحقق من الصلاحيات:**
```javascript
// التحقق من دور المستخدم
if (user.role === 'admin') {
  // صلاحيات المدير
}

// التحقق من الصلاحية المحددة
if (hasPermission('manage_users')) {
  // عرض إدارة المستخدمين
}
```

## 🧪 اختبار النظام

### **سيناريوهات الاختبار:**

#### 1. **تسجيل الدخول:**
- ✅ دخول صحيح بصفة مدير
- ✅ دخول صحيح بصفة مستخدم
- ❌ دخول ببيانات خاطئة
- ❌ دخول بحساب غير مفعل

#### 2. **إنشاء الحساب:**
- ✅ إنشاء حساب ببيانات صحيحة
- ❌ إنشاء حساب باسم مستخدم موجود
- ❌ إنشاء حساب ببريد إلكتروني موجود
- ❌ إنشاء حساب ببيانات ناقصة

#### 3. **الصلاحيات:**
- ✅ وصول المدير لجميع الصفحات
- ✅ وصول المستخدم للصفحات المسموحة
- ❌ منع المستخدم من صفحات المدير
- ❌ منع الضيف من الصفحات المحمية

#### 4. **الجلسات:**
- ✅ استمرار الجلسة بعد إعادة تشغيل التطبيق
- ✅ انتهاء الجلسة بعد 24 ساعة
- ✅ تسجيل الخروج ينهي الجلسة
- ✅ جلسة واحدة لكل مستخدم

## 🔮 التطوير المستقبلي

### **المرحلة التالية:**
- [ ] صفحة إدارة المستخدمين
- [ ] صفحة إدارة التراخيص
- [ ] صفحة إنشاء التراخيص
- [ ] تقارير متقدمة للمدير
- [ ] إعدادات النظام المتقدمة
- [ ] سجل النشاطات المفصل

### **تحسينات مستقبلية:**
- [ ] تسجيل دخول بالبريد الإلكتروني
- [ ] استعادة كلمة المرور
- [ ] تفعيل الحساب عبر البريد
- [ ] جلسات متعددة للمستخدم الواحد
- [ ] صلاحيات مخصصة ومرنة
- [ ] تسجيل دخول بالبصمة/الوجه

## 📝 ملاحظات مهمة

### **الأمان:**
- غير كلمات المرور الافتراضية في الإنتاج
- استخدم HTTPS في البيئة الإنتاجية
- راجع صلاحيات المستخدمين دورياً

### **الصيانة:**
- نظف الجلسات المنتهية دورياً
- راجع سجل النشاطات
- احتفظ بنسخ احتياطية من ملفات المستخدمين

### **الأداء:**
- الجلسات محفوظة في ملف JSON (مناسب للاستخدام المحدود)
- فكر في قاعدة بيانات للاستخدام الكثيف
- راقب حجم ملفات الجلسات والمستخدمين

---

**🎉 نظام مصادقة شامل وآمن!**

*تحكم كامل في المستخدمين والصلاحيات مع أمان متقدم* 🔐👥🚀
